# Fine-tune

Mini practice projects for learning LLM fine-tuning and evaluation in Python.

## What’s in here (planned)
- Data prep (JSONL)
- Baseline vs fine-tuned evals
- Fine-tuning job scripts
- Simple scoring (JSON validity, schema checks)

## How to run
- `python scripts/make_dataset.py` generates JSONL training data in `data/`
- `python scripts/eval_json.py` runs basic JSON/schema checks